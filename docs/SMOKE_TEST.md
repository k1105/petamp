# スモークテスト チェックリスト

リファクタリング各フェーズの完了ゲート。自動テストがないため、該当機能に触れた PR では必ず通すこと。
（機械ゲート: `npm run build` 成功 + `npm run lint` 0 errors + `npx knip` が前フェーズより悪化していない）

## Web（npm run dev で確認可能な範囲）

### 起動・ギャラリー
- [ ] 起動 → ローディング → ギャラリー（map ビュー）が表示される
- [ ] 未ログイン時にオンボーディング/ログインへ誘導される
- [ ] リストパネル: TRAIL タブにラン一覧（co-run 統合タイル含む）が出る
- [ ] リストパネル: ISLAND タブに島レイアウトが描画される
- [ ] movement type フィルタ（足/自転車等）の切り替えが効く
- [ ] FAB 長押し → armed 状態 → 記録開始に遷移できる
- [ ] プロフィールパネルが開閉できる
- [ ] ペタンプの吹き出し・metaball が FAB に追従して描画される

### 記録フロー
- [ ] 記録開始 → 現在位置追従（3D pitch）で軌跡が伸びる
- [ ] 記録終了 → PostRunLoading → RunResult（軌跡スケッチ + 第一声）
- [ ] RunResult → チャット → 会話 → 終了で保存され、ギャラリーに反映される

### ラン詳細
- [ ] RunDetail: リプレイ再生（再生/停止・動点・タップフラッシュ）
- [ ] RunDetail: nutrition タブの表示
- [ ] RunDetail: co-run ランで相手の軌跡とアバターが色分け表示される
- [ ] エリア名（areaName）が表示される

### ソーシャル・連携
- [ ] フレンド招待: QR/リンク生成、受け側で /invite が開く
- [ ] co-run: 招待 → ロビー → 同時記録開始
- [ ] Spotify: 接続 → 再生中トラックが反映される（BPM ドット）
- [ ] 設定パネル: テーマ切替・実験フラグの永続化（リロード後も保持）

## iOS 実機のみ

- [ ] バックグラウンドで記録が継続する（画面ロック 5 分以上）
- [ ] Live Activity が記録中に表示・更新される
- [ ] ネイティブ Firestore 経由の保存（記録保存後、別端末/Webで同期確認）
- [ ] Universal Links: 招待リンクからアプリが直接起動する
- [ ] Sign in with Apple / Google ログイン

## 長時間稼働テスト（TestFlight 配布前・リソースリーク関連の変更後）

- [ ] 30 分連続記録 → 終了 → 保存 → ギャラリー反映。途中でクラッシュ・メモリ膨張・地図フリーズがないこと
